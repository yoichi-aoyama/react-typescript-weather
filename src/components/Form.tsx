
const Form = () => {
    return (
        <form>
            <input type="text" name="city" placeholder="都市名" />
            <button type={"submit"}>天気取得</button>
        </form>
    );
};

export default Form;